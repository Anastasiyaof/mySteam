language: node_js
node_js:
- '10'

before_install:
- npm install -g @angular/cli
install: npm install

branches:
  only:
  - master


deploy:
  provider: heroku
  app: mysteampro
  api_key:
    secure: travis encrypt $(heroku auth:token) --add deploy.api_key